(def 'defm 
  (macro ['name 'params 'body] 
    (def name (macro params body))))
(def 'defn 
  (macro ['name 'params 'body] 
    (def name (fn params body))))

(defm 'compose ['fn1 'fn2]
  (fn ['x] (fn2 (fn1 x))))

(def 'kek (fn ['a 'b] (+ a b)))
